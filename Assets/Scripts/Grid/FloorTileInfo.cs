using UnityEngine;

/// <summary>
/// Stores the grid position for a floor tile, enabling raycast-based grid lookups.
/// </summary>
public class FloorTileInfo : MonoBehaviour
{
    public Vector2Int GridPosition;
}
